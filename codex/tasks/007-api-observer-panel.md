Task: Build read-only protocol dashboard (/system)

Scope:
- Repo: AIMAS-UI only
- Visualize Input → Capsule → Fit Matrix → Paths

Requirements:
- No API calls required; deterministic sample view only
- Accept optional `intent` query to show declared input
- Highlight Free Baseline Path requirement in UI copy

Output:
- File diffs only
- No prose
