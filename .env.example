# AIMAS-UI example environment configuration
# Copy to .env.local and adjust values per deployment.

# Public Cloud Run endpoint for the AIMAS API
NEXT_PUBLIC_AIMAS_API_BASE=https://aimas-api-936759581716.us-central1.run.app

# Server-only adapter key (used by /api/adapter/normalize). Never expose client-side.
OPENAI_API_KEY=sk-xxxx

# Optional docs override path (defaults to vendor/aimas or AIMAS/)
AIMAS_DOCS_PATH=./AIMAS

# Dev-only escape hatch to skip OpenAPI check during docs:verify
AIMAS_ALLOW_MISSING_OPENAPI=0
